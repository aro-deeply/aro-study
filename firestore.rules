rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인(공용 계정)한 사용자만 모든 컬렉션을 읽고 쓸 수 있다.
    // 이것이 실제 접근 제한이다. 정적 HTML에는 민감한 내용을 두지 않는다.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
