# HR STUDY 모임 기록 사이트: 사용 안내

작성 2026-09-08, 갱신 2026-09-10. 총무(ARO)가 순서대로 보기 위한 문서다. 2026-09-10 멤버의 비밀번호 로그인을 없앴고(이름 선택만), 관리 화면만 총무 비밀번호를 쓴다. 비밀번호는 여기에 적지 않는다.

## 0. 지금 상태 (2026-09-10)

- 끝난 것: Firebase 규칙 게시, 총무 등록, 멤버 등록(11명), 사이트 전체 동작 확인.
- 2026-09-09 첫 모임(OT)에서 정한 운영 방식이 사이트에 반영됐다: 매월 둘째 수요일 정기 모임, 발제 순서(9월 발제자를 뺀 10명, 10월부터 순환), 1인 회비 50,000원을 6개월로 나눠 월별 가용 금액 안에서 쓰고 덜 쓰면 이월, OT 비용은 참석자 n분의 1.
- 2026-09-10 데이터 입력 완료: 모임 규칙(둘째 수요일), 발제 순서 10명(10월 시작, 9월 발제자는 세션에 확정값으로 기록), 회비 설정(1인 50,000원, 2026-10부터 6개월, 대상 10명), OT 세션(2026-09-09, 참석 10명)과 지출 5건 149,300원(참석자 n분의 1, 낸 사람 총무). 1월 합류 예정 멤버 1명은 회비 대상과 OT 참석자에서 뺐다(멤버 목록과 발제 순서에는 있음). 로그인된 브라우저에서 Claude가 넣었다. 같은 내용의 JSON(`../work/aro-study/setup-2026-09.json`)과 `scripts/setup.mjs`는 다음에 비슷한 일괄 입력이 필요할 때 쓴다(4-0절).
- 2026-09-10 접속 방식 변경: 멤버는 주소를 열면 바로 "이름 선택"이 뜬다(비밀번호 없음). 관리 화면(admin.html)만 총무 비밀번호(예전 공용 비밀번호와 같음)를 기기당 한 번 묻는다. 멤버·회비·세션·지출 같은 관리 데이터는 Firestore 규칙에서 총무 계정만 고칠 수 있고, 멤버는 의견·메모·관련 자료만 올리고 본인 것만 지운다. 주소를 아는 사람은 누구나 볼 수는 있으므로 주소는 멤버에게만 알린다(8절).
- 2026-09-10 저녁: 새 규칙(3-1, 3-4)을 Claude가 크롬에서 게시했고, 익명 사용자로 검증했다(관리 데이터 쓰기 거부, 의견은 본인 uid로만 생성·삭제). 총무 비밀번호도 같은 날 재설정 메일로 바꿨다(어디에도 기록하지 않음).
- 남은 것 (총무가 할 일):
  1. 계좌를 개설하면 admin 회비 탭 > 회비 설정 "수정" > "입금 계좌"에 넣는다. 회비 현황에 표시된다.
  2. OT 지출 중 다른 사람이 결제한 항목이 있으면 admin 지출 탭에서 "수정"해 "낸 사람"을 바꾼다. 참석자가 11명 전원이 아니면 세션 탭에서 체크를 고친다.
  3. 첫 회비 입금이 들어오면 회비 탭에서 "납부 처리". OT 정산금(1인 15,000원)을 받으면 송금 기록 탭에 남긴다.
  5. 1월에 합류하는 멤버가 회비를 내면: 회비 탭 > 회비 설정 "수정" > 대상 멤버 "직접 선택"에서 그 멤버를 체크해 저장한 뒤 "납부 처리". 월별 가용 금액은 그때부터 총액이 늘어난 기준으로 다시 계산된다.
  4. OT 기록 페이지(`weeks/2026-09-09/`)는 2026-09-10에 만들었다(운영 결정 + 브리프 03 링크). 발제·토론 내용은 메모를 받으면 채운다(`../work/aro-study/2026-09-09/content.html` 수정 후 `python scripts/new_week.py --date 2026-09-09 ... --force`).

## 1. 주소 모음

| 용도 | 주소 | 로그인 |
|---|---|---|
| 사이트(회비 현황·주차 목록) | https://aro-deeply.github.io/aro-study/ | 없음(이름 선택만) |
| 총무 관리 화면 | https://aro-deeply.github.io/aro-study/admin.html | 없음(총무 이름 선택) |
| 목록 화면 예시(가명 데이터) | https://aro-deeply.github.io/aro-study/?demo=1 | 없음 |
| 주차 페이지 예시(하이라이트·메모·의견 시험 가능) | https://aro-deeply.github.io/aro-study/demo/ | 없음 |
| GitHub 저장소 | https://github.com/aro-deeply/aro-study | GitHub 계정 |
| Firebase 콘솔 | https://console.firebase.google.com (프로젝트 hr-study-54acd) | Google 계정 |

예시 페이지 두 개는 실제 데이터와 무관하다. 화면 구성만 보는 용도이고, 거기서 남긴 메모·의견은 저장되지 않는다.

## 2. 사이트 구조 한눈에

- 멤버는 비밀번호 없음. 주소를 열면 "이름 선택"이 뜨고 본인을 고르면 의견·메모·자료가 그 이름으로 기록된다. 기기마다 한 번만 하면 된다. (내부적으로 Firebase "익명 로그인"이라는 기기 토큰이 자동으로 만들어지는데, 멤버에게는 보이지 않고 이름 선택만 보인다.) 주소를 아는 사람은 누구나 볼 수 있으므로 주소는 멤버에게만 알린다.
- 총무: "이름 선택"에서 총무로 등록된 이름을 고르면 상단에 "관리" 링크가 생기고, 관리 화면에 처음 들어갈 때 총무 비밀번호를 한 번 묻는다(기기에 유지). 다른 사람이 총무 이름을 골라도 비밀번호가 없으면 관리 화면에 못 들어가고, Firestore 규칙이 관리 데이터 쓰기를 막는다.
- 화면은 세 종류다.
  - 첫 화면(요약 대시보드 + 탭 4개, 2026-09-10 개편): 맨 위에 다음 모임·발제자·회비 잔액·이달 가용 카드. "요약" 탭은 다음 모임 카드, 확인할 것(미납, 보전할 돈, 남은 정산), 최근 모임 3개. "월별" 탭은 달을 골라 그 달의 모임(날짜·발제·장소·기록 링크), 회비 배정·이월·지출·남은 금액, 지출 내역, 납부, 회차별 n분의 1 정산서를 한 화면에서 본다. "회비" 탭은 잔액·월별 가용 금액 표·멤버별 납부. "일정·발제" 탭은 12개월 표와 발제 순서. 탭과 달은 주소(#months/2026-10)에 남아 그대로 공유할 수 있다.
  - 주차 페이지(`weeks/<날짜>/`): 이날의 내용, 브리프 링크, 다음 모임 계획, 이 회차 비용, 의견, 본문 하이라이트·메모. 모임 후 Claude Code로 만든다(5절).
  - 관리 화면(admin.html): 총무만. 멤버, 일정·발제, 회비, 세션, 지출, 송금 기록 6개 탭.
- 스터디 내용 기록은 관리 화면이 아니라 주차 페이지에서 한다. 내용 요약과 브리프 링크는 총무가 Claude Code로 만들고, 의견·하이라이트·메모는 멤버들이 그 페이지에서 직접 남긴다. 관리 화면 세션 탭의 "총무 메모"는 총무만 보는 짧은 메모 자리다.

## 3. 처음 한 번만 하는 설정 (끝났음, 다시 할 때 참고)

### 3-1. Firestore 규칙 게시

1. Firebase 콘솔에서 프로젝트 `hr-study-54acd`를 누른다.
2. 왼쪽 "프로젝트 바로가기" 아래 "Firestore"를 누른다. (예전 콘솔은 "빌드" 아래 "Firestore Database")
3. 위쪽 탭 "규칙"을 누르고 편집창 내용을 전부 지운 뒤 저장소의 `firestore.rules` 내용을 그대로 붙여 넣고 "게시". (2026-09-10 새 규칙: 아래 요약)

의미
- 읽기: 접속한 사람 누구나(멤버의 익명 접속 포함). 비밀번호가 없으므로 주소를 아는 사람은 볼 수 있다.
- 멤버·설정·회비·세션·지출·송금 기록 쓰기: 총무 계정(`aro.deeply@gmail.com`으로 로그인)만.
- 의견·하이라이트·관련 자료: 누구나 올리고, 고치거나 지우는 것은 올린 본인(같은 기기 토큰) 또는 총무만.
- 총무 이메일을 바꾸면 `assets/firebase-config.js`의 `ADMIN_EMAIL`, `firestore.rules`, `storage.rules` 세 곳을 같이 바꾼다.

### 3-2. Authentication (2026-09-10 변경)

두 가지가 켜져 있어야 한다. 2026-09-10 기준 둘 다 켜져 있다.

- "익명"(Anonymous): 멤버가 비밀번호 없이 들어올 때 쓰는 기기 토큰. Claude가 크롬에서 켜고 저장했다. "자동 정리 사용 설정"(30일 지난 익명 계정 삭제)은 켜지 않는다. 켜면 한 달 뒤 기기의 접속이 끊겨 권한 오류가 날 수 있다.
- "이메일/비밀번호": 총무 계정(`aro.deeply@gmail.com`) 로그인용. 2026-09-08부터 켜져 있다. Users 탭에 이 계정이 있어야 한다.

다시 켜야 할 때: https://console.firebase.google.com/project/hr-study-54acd/authentication/providers (콘솔 > Authentication > "로그인 방법" 탭) > 목록에서 항목을 누르거나 "새 제공업체 추가" > "사용 설정" 켜고 "저장".

참고
- Users 탭에 기기마다 "익명" 사용자가 하나씩 쌓인다. 정상이고 지워도 된다(그 기기는 다음 방문 때 새로 만들어진다. 다만 지우면 그 기기에서 올린 의견·자료를 본인이 지울 수 없게 되고 총무만 지울 수 있다).
- 사이트를 새로고침해 "익명 로그인이 꺼져 있음" 안내가 뜨면 "익명"이 꺼진 것.
- 총무 비밀번호를 바꾸려면 Users 탭 > 계정 오른쪽 메뉴 > "비밀번호 재설정". 멤버에게 알릴 필요는 없다(총무만 쓴다).

### 3-4. Storage 켜기 (관련 자료 파일 첨부용, 2026-09-10 추가)

2026-09-10 완료(Blaze 전환, 버킷 US-EAST1, 규칙 게시, 업로드 확인). 아래는 다시 할 때 참고.

파일 첨부는 Firebase Storage에 올린다. 2024년 10월 30일 이후 만든 프로젝트는 Storage를 켜려면 요금제를 Blaze(종량제)로 바꿔야 한다. 무료 한도(저장 5GB, 다운로드 하루 1GB)가 포함돼 스터디 규모에서는 청구가 거의 0원이지만 카드 등록이 필요하다. 예산 알림을 걸어 두면 안전하다.

1. Firebase 콘솔 > 프로젝트 `hr-study-54acd` > 왼쪽 아래 "업그레이드"(또는 설정 > 사용량 및 결제) > Blaze 선택 > 결제 계정 만들기(카드 등록). 예산 알림 금액을 1,000원 정도로 설정.
2. https://console.firebase.google.com/project/hr-study-54acd/storage > "시작하기" > 버킷 옵션은 "무료 위치"(US-EAST1) 그대로(무료 한도는 미국 위치에만 적용) > 보안 규칙 "프로덕션 모드" > 완료.
3. Storage 화면 위쪽 "규칙" 탭 > 편집창을 지우고 저장소의 `storage.rules` 내용을 붙여 넣고 "게시". (2026-09-10 새 규칙: 올리기는 누구나 20MB 이하, 지우기는 올린 본인 또는 총무만)
4. 사이트의 아무 기록 페이지 > 관련 자료 > 파일을 골라 "올리기". 목록에 파일 이름과 크기가 뜨고 눌러서 열리면 끝. 실패하면 "파일 저장소가 아직 준비되지 않음" 안내가 뜬다(규칙 게시 여부 확인).

### 3-3. 총무 등록

admin.html에서 "이름 없이 계속" > 총무 비밀번호 입력 > 멤버 탭에서 본인 이름을 "총무 (관리 권한)" 체크와 함께 저장 > 상단 "이름 선택"에서 본인을 고른다. "관리" 링크가 생기면 끝. 2026-09-09 완료.

## 4. 총무의 평소 사용 (admin.html)

### 4-0. 운영 데이터 한 번에 넣기 (scripts/setup.mjs)

2026-09-10에 정한 내용(모임 규칙, 발제 순서, 회비 설정, OT 세션과 지출 5건)은 `C:\Users\user\Desktop\hr-brief\work\aro-study\setup-2026-09.json`에 준비돼 있다. 저장소 밖 파일이라 GitHub에 올라가지 않는다.

1. 시작 메뉴에서 PowerShell을 열고 아래를 한 줄씩 입력한다.
   ```
   cd "C:\Users\user\Desktop\hr-brief\HR study"
   node scripts/setup.mjs ../work/aro-study/setup-2026-09.json --dry-run
   ```
2. "총무 비밀번호:"가 뜨면 총무 비밀번호(관리 화면과 같은 것)를 입력하고 Enter (화면에 글자가 안 보이는 게 정상).
3. "넣을 내용" 목록이 나온다. 멤버 이름이 admin에 등록된 이름과 다르면 `멤버를 찾을 수 없음: "이름"`으로 멈추므로, admin 멤버 탭에서 이름을 맞추거나 JSON의 이름을 고친 뒤 다시 실행한다.
4. 목록이 맞으면 `--dry-run`을 빼고 다시 실행한다. 비밀번호를 넣고, "n건을 Firestore에 쓸까요? (y/N)"에 `y` Enter.
5. "끝."이 나오면 사이트를 새로고침한다. 첫 화면 상단에 "다음 모임 10월 14일 (수)"와 10월 발제자 이름(순서 1번), 회비 현황에 월별 가용 금액 표, 주차 목록에 OT 카드(지출 149,300원), 맨 아래 "추가 비용 정산" 표가 보이면 정상.
6. 두 번 실행해도 이미 있는 회비 설정·지출은 "이미 있음, 건너뜀"으로 표시되고 중복되지 않는다.

JSON에서 미리 정해 둔 가정(틀리면 admin에서 고치면 된다): 회비 기간은 2026년 10월부터 6개월(2027년 3월까지, 9월 OT는 회비 밖), OT 참석자는 활성 멤버 전원, OT 지출 5건의 낸 사람은 총무.

### 4-1. 운영 방식

- 정기 모임은 매월 둘째 수요일. 목록 화면의 "다음 모임"은 이 규칙으로 자동 계산되고, 그 달에 세션을 만들면 세션 날짜가 우선한다. 규칙은 일정·발제 탭에서 바꾼다.
- 발제는 정한 순서대로 매월 한 명. 순서가 끝나면 미정으로 표시된다(새 순서를 다시 정한다). 특정 달만 다른 사람이 하면 세션 탭에서 그 세션의 발제자를 고른다.
- 1인 회비를 걷어 총무가 보관하고, 모임 비용은 회비에서 쓴다. 잔액 = 납부 합계 - 회비 지출 합계.
- 월별 가용 금액: 회비 총액(1인 회비 x 대상 인원, 예: 50,000 x 10 = 500,000원)을 사용 개월 수(6)로 나눠 매월 배정하고, 덜 쓴 금액은 다음 달로 이월된다. 월 배정액은 100원 단위 올림(500,000 / 6 = 83,333.3 → 83,400원)이고 마지막 달은 남는 금액(83,000원)이다. 이달 가용 = 이달 배정 + 이월 - 이달 지출. 이 계산은 계획 총액 기준이라 미납자가 있으면 실제 현금(잔액 카드)은 그보다 적다.
- 총무가 아닌 사람이 모임 비용을 대신 결제하면 "회비에서 보전할 돈"으로 표시되고, 총무가 돌려준 뒤 송금 기록을 남기면 사라진다.
- 회비를 넘는 추가 비용과 OT처럼 회비 밖으로 정한 비용만 참석자끼리 n분의 1로 나눈다. 총액을 인원으로 나눠 100원 단위로 올리고(149,300 / 10 = 14,930 → 15,000원), 올림으로 더 걷히는 차액(700원)은 총무가 받아 회비 수입으로 들어간다. 총무 부담액은 다른 멤버와 같고, 정산서에는 이 차액이 "회비 적립" 카드로 따로 표시된다. 이런 지출이 없으면 개인 정산 화면은 아예 나오지 않는다.

### 4-1-1. 일정·발제 탭

- 정기 모임 규칙: 몇째(첫째~넷째, 마지막), 요일, 시간, 장소. 저장하면 목록 화면 상단과 "일정과 발제" 표가 바뀐다.
- 발제 순서: "시작 월"을 고르고 멤버를 순서대로 넣는다("활성 멤버를 멤버 순서대로 채우기" 후 위로/아래로/빼기로 조정). 시작 월의 발제자가 1번. 아래 미리보기에서 12개월치 날짜와 발제자를 확인하고 저장.
- 멤버가 빠지거나 새로 들어오면 여기서 순서를 고친다. 이미 지난 달은 세션의 발제자로 남아 있으므로 순서를 바꿔도 과거 기록은 바뀌지 않는다(세션에 발제자가 저장된 경우).

### 4-2. 회비 탭

1. 처음 한 번: 아래쪽 "회비 설정 만들기"에서 1인 회비 금액, 시작일, 사용 개월 수(예: 6)를 넣고 저장한다. 종료일은 비우면 개월 수로 계산된다. 입금 계좌를 넣으면 회비 현황에 표시된다. 대상은 기본 "활성 멤버 전원"이라 나중에 추가한 멤버도 자동으로 들어간다.
2. 입금을 확인할 때마다 위쪽 납부 현황에서 그 사람 옆 "납부 처리"를 누른다. 오늘 날짜로 1인 회비 전액이 기록된다.
3. 금액이나 날짜가 다르면(나눠 냈거나 지난 날짜) "납부 기록 직접 입력"을 쓴다. 일부만 냈으면 "일부"로 표시되고 남은 금액이 보인다.
4. 잘못 눌렀으면 그 사람 옆 "취소". 회비 금액이나 기간이 바뀌면 회비 설정 목록에서 "수정". 다음 회비를 새로 걷을 때는 설정을 하나 더 만들면 된다(이전 설정은 목록 화면 아래 "이전 회비 설정"으로 남는다).

### 4-3. 세션 탭

모임 날짜(= 세션 ID = 주차 페이지 폴더명), 제목, 발제자(날짜를 고르면 순서상 발제자가 자동으로 들어가고 바꿀 수 있다), 참석자 체크, 다음 모임 계획, 총무 메모. 모임 당일 만들어 두면 지출 입력이 바로 된다. 세션을 삭제하면 딸린 지출·송금 기록도 함께 지워진다(확인창에 건수 표시). 의견·하이라이트는 남는다.

### 4-4. 지출 탭

위에서 세션을 고른 뒤 항목, 금액(숫자 키패드), 날짜, 카테고리(식사·카페·장소·기타), 재원, 낸 사람, 비고.

- 재원 기본값은 "회비에서". 개인 정산에 잡히지 않고 잔액만 줄어든다.
- 총무가 아닌 사람이 대신 결제했으면 "낸 사람"을 그 사람으로 둔다. 목록 화면에 "회비에서 보전할 돈"으로 뜬다.
- 회비 밖 추가 비용은 재원을 "참석자 n분의 1"로 바꾸고 분배 대상(기본 참석자 전원)을 고른다.
- 목록에서 "수정"을 누르면 고치거나 삭제할 수 있다.

### 4-5. 송금 기록 탭

- 대신 결제한 사람에게 회비에서 돌려줬을 때: 보낸 사람 "회비 (총무 보관)", 받은 사람 그 멤버.
- 추가 비용을 멤버끼리 보냈을 때: 보낸 사람과 받은 사람.
- 기록하면 "보전할 돈"과 "보낼 돈"에서 빠지고 완료 표시가 붙는다.

### 4-6. 멤버 탭과 총무 변경

- 멤버 추가는 이름만 넣으면 된다. 순서는 비워 두면 자동. 비활성화한 멤버는 이름 선택과 참석자 목록에서 빠지지만 과거 기록에는 남는다.
- 총무 변경: 새 총무의 "수정"을 눌러 "총무 (관리 권한)"을 체크해 저장하고, 본인 항목의 체크를 해제한다. 새 총무는 자기 기기에서 "이름 선택"으로 본인을 고르면 "관리" 링크가 생기고, 관리 화면에 들어갈 때 총무 비밀번호를 넣는다(비밀번호는 전 총무에게 받거나 Firebase 콘솔에서 재설정).
- 총무는 회비 보관자로 계산된다. 총무가 바뀌면 그 뒤로 "보전할 돈"과 "회비 적립분 수령"이 새 총무 기준이 된다. 총무가 두 명이면 순서가 앞선 사람이 보관자로 계산되므로 평소에는 한 명만 둔다. 실제 계좌의 돈을 옮기는 것은 사이트 밖의 일이고 잔액 숫자는 그대로 이어진다.

## 5. 모임 후 기록 페이지 만들기 (Claude Code)

1. 이 폴더(`HR study`)에서 Claude Code를 연다.
2. 모임 메모(텍스트나 음성 전사)를 붙여 넣고 "이번 주 기록 만들어"라고 한다. 날짜와 제목을 물어보면 답한다.
3. Claude가 `weeks/<날짜>/index.html`을 만들고(내용 요약, 관련 브리프 링크 카드, 다음 모임 계획), Firestore 세션을 등록하고(이때 총무 비밀번호를 터미널에서 한 번 묻는다, 화면에 표시되지 않음), 커밋·푸시한 뒤 주소를 알려 준다.
4. 1~2분 뒤 목록 맨 위 카드가 링크로 바뀌면 끝. 참석자 체크나 지출 입력이 안 되어 있으면 admin에서 마저 한다.
5. 멤버들은 그 페이지에서 본문을 드래그해 하이라이트·메모를 남기고, "관련 자료"에 발제 주제와 관련된 링크·파일(20MB 이하)·메모를 올리고, 아래 "의견"에 댓글과 답글을 단다. 파일 첨부는 3-4절의 Storage 설정이 끝나야 동작한다.

메모 원문은 `../work/aro-study/<날짜>/`에 남고(저장소 밖, 비공개), 저장소에는 정리본만 들어간다. 주차 페이지 본문에는 멤버 이름이나 민감한 내용을 쓰지 않는다(저장소가 공개라서).

## 6. 멤버에게 보낼 안내 (복사해서 쓰면 됨)

> 스터디 기록 사이트 주소: https://aro-deeply.github.io/aro-study/
> 비밀번호는 없습니다. 주소는 멤버끼리만 공유해 주세요. (회비·일정 입력은 총무만 할 수 있습니다.)
> 그다음 "이름 선택"에서 본인 이름을 고르세요. 의견과 메모가 그 이름으로 기록됩니다.
> 첫 화면에서 회비 납부 현황과 잔액을 볼 수 있습니다.
> 핸드폰에서는 브라우저 메뉴의 "홈 화면에 추가"를 누르면 앱처럼 쓸 수 있습니다.
> 각 주차 페이지에서 본문을 드래그하면 하이라이트·메모를 남길 수 있고, 아래 "의견"에 댓글을 달 수 있습니다.

## 7. 확인 체크리스트

끝난 것은 표시해 두었다. 나머지는 시간 날 때 위에서부터.

확인 완료 (2026-09-09)
- [x] (2026-09-10 이전 방식) 로그인 화면에 비밀번호 칸 하나만 보인다. 이후 비밀번호 제거.
- [x] 로그인 후 검은 "권한 오류" 토스트가 뜨지 않는다.
- [x] admin에서 본인을 총무로 추가하고 이름을 고르면 상단에 "관리" 링크가 생긴다.
- [x] 세션을 만들고 지출을 넣으면 합계·차액 계산이 맞다. (n분의 1 방식으로 확인, 이후 회비 방식으로 변경)
- [x] 회비 탭에서 회비 설정을 만들면 멤버 전원이 "미납"으로 뜨고, "납부 처리"를 누르면 "납부"로, "취소"하면 다시 "미납"으로 바뀐다. 설정 삭제도 된다. (확인용 설정으로 시험 후 삭제)

아직 안 한 것
- [x] 시크릿 창에서 주소를 열면 비밀번호 없이 바로 "이름 선택"이 뜬다. (2026-09-10 로컬 확인)
- [ ] 새 규칙 게시 후: 총무가 아닌 이름으로 admin.html에 들어가면 "총무 전용 화면" 안내만 보이고, 총무 이름을 골라도 비밀번호를 모르면 못 들어간다.
- [ ] 새 규칙 게시 후: 멤버 기기에서 의견·자료를 올리고 본인 것만 삭제 버튼이 보이며 삭제된다. 총무 기기에서 관리 화면 입력이 전처럼 된다.
- [ ] 총무가 아닌 이름으로 admin.html을 열면 "총무만 들어올 수 있는 화면입니다" 안내만 보인다. (시크릿 창에서 확인)
- [ ] 예시 페이지(demo)에서 본문을 드래그하면 "하이라이트 / 메모" 버튼이 뜨고, 의견을 남기고 답글·삭제가 된다.
- [ ] 핸드폰에서 첫 화면(회비 잔액 카드, 납부 표, 주차 목록)이 한 줄로 잘 접혀 보인다.
- [ ] 핸드폰 브라우저 "홈 화면에 추가"가 되고, 아이콘으로 열면 주소창 없이 뜬다.
- [ ] 실제 회비 설정을 만든 뒤: 목록 화면 상단 미납 인원과 회비 현황 표가 admin의 납부 처리와 같이 바뀐다.
- [ ] 첫 모임 후: 세션과 지출을 넣으면 목록 화면 회비 잔액이 그만큼 줄고, 주차 카드에 지출 합계가 뜬다.
- [ ] 지출의 "낸 사람"을 총무가 아닌 멤버로 두면 "회비에서 보전할 돈"에 뜨고, 송금 기록(보낸 사람 "회비")을 남기면 사라진다.
- [ ] 재원을 "참석자 n분의 1"로 넣은 지출이 있을 때만 목록 아래 "추가 비용 정산" 표가 나타난다.
- [ ] 두 기기 확인: 핸드폰에서 남긴 의견이 노트북 화면에 새로고침 없이 나타난다. (주차 페이지가 있어야 하므로 첫 기록 후에 확인)

## 8. 결정하거나 검토할 것 (총무 판단)

- 회비 조건(2026-09-10 확정): 1인 50,000원, 6개월 분할 사용, 덜 쓴 금액 이월. 미확정: 회비 기간의 시작 월(JSON은 10월로 가정), 입금 계좌(개설 후 입력), 6개월 뒤 남는 돈의 처리(이월·환급), 중도 합류·탈퇴 멤버의 회비.
- 발제 순서: 9월에 이미 발제한 사람은 순서에서 빼고 10명이 10월부터 2027년 7월까지. 그 뒤는 "미정"으로 표시되므로 2027년 여름에 일정·발제 탭에서 새 순서와 시작 월을 정한다.
- 멤버 비밀번호 제거(2026-09-10 결정): 고치는 것은 규칙으로 막았지만(관리 데이터는 총무만, 의견·자료는 본인 것만), 보는 것은 주소를 아는 사람 누구나 가능하다(멤버 이름·납부 현황·입금 계좌·의견). 계좌번호는 회비 설정에 넣지 않고 단톡방으로만 알리는 것이 안전하다. 멤버 비밀번호를 되살리려면 git 기록(2026-09-10 이전)의 `assets/app.js`를 참고한다.
- 관리 권한 검증 수준(2026-09-10 강화): admin 진입은 총무 이름 + 총무 비밀번호이고, 관리 데이터 쓰기는 Firestore·Storage 규칙이 총무 계정만 허용한다. 개발자 도구를 써도 총무 비밀번호 없이는 못 고친다. 의견·자료를 지우는 것은 본인 기기 토큰 기준이라, 브라우저 데이터를 지운 뒤에는 예전 글을 본인이 못 지우고 총무만 지울 수 있다.
- 아이콘: 지금은 aro-briefs와 같은 아이콘을 쓴다. 홈 화면에서 두 앱을 구분하고 싶으면 `assets/icons/`의 PNG 3장을 바꾸면 된다.
- 카테고리: 식사·카페·장소·기타 4개. 바꾸려면 `admin.html`의 카테고리 목록과 `assets/settle.js`의 `CATEGORIES`를 같이 고친다.
- 공개 범위: 저장소는 공개(GitHub Pages 조건)다. 정적 HTML에는 모임 내용 요약만 들어가고 이름·금액·의견은 Firestore에만 있으므로, 주차 페이지 본문에 멤버 이름이나 민감한 내용을 쓰지 않는 원칙을 지키면 된다.

## 9. 문제 해결

- "권한 오류: Firestore 규칙을 확인하세요": 3-1 규칙이 풀린 것. 다시 게시 후 새로고침.
- "Firestore 인덱스가 필요합니다" 안내와 함께 콘솔 링크가 열림: 링크 페이지에서 "인덱스 만들기"를 누르고 몇 분 기다린 뒤 새로고침. (현재 설계에서는 필요 없게 만들었지만 혹시 뜨면 이렇게 한다.)
- 이름 목록이 비어 있음: admin에서 멤버를 아직 안 넣은 것. "이름 없이 계속"으로 들어가 admin에서 추가.
- 첫 화면에 "아직 회비 설정이 없습니다": admin 회비 탭에서 1인 회비를 아직 안 넣은 것. 4-2 참고.
- 주차 페이지가 404: 푸시 후 1~2분 안 지났거나, 세션만 만들고 기록 페이지(5절)를 아직 안 만든 것. 목록에서 "기록 작성 전" 태그가 그 상태다.
- 화면이 예전 것처럼 보임: 브라우저 캐시. 새로고침을 두 번 하거나 시크릿 창으로 확인.
- "익명 로그인이 꺼져 있음" 안내: 3-2절대로 콘솔에서 "익명"을 켠 뒤 "다시 시도".
- 총무 비밀번호를 잊음: Firebase 콘솔 > Authentication > Users > 계정 오른쪽 메뉴 > "비밀번호 재설정".
- 관리 화면에서 저장이 "권한 오류"로 실패: 총무 계정 로그인이 풀린 것(관리 화면을 새로고침해 비밀번호 입력) 또는 규칙의 이메일이 계정과 다른 것.
- 실수로 지출을 잘못 넣음: admin 지출 탭에서 해당 항목 "수정" > "삭제". 납부를 잘못 처리했으면 회비 탭에서 그 사람 옆 "취소".
- 세션을 지웠는데 잔액이나 정산에 숫자가 남아 있음: 2026-09-09 이후 버전에서는 세션 삭제 시 지출·송금 기록이 함께 지워지므로 생기지 않는다. 그래도 남아 있으면 같은 날짜로 세션을 다시 만들면 지출 탭에 나타나므로 거기서 지우고 세션을 다시 삭제한다.

## 10. 파일 위치 (참고)

- 이 폴더: `C:\Users\user\Desktop\hr-brief\HR study` (저장소 루트)
- 설계 원문 `SPEC.md`, 작업 규칙 `CLAUDE.md`, 사용 안내 `README.md`, 이 문서 `GUIDE.md`
- 화면 미리보기(로컬): 이 폴더에서 `python -m http.server 8770` 실행 후 브라우저에서 `http://127.0.0.1:8770/`. 파일을 더블클릭해서 여는 방식(file://)으로는 동작하지 않는다.
- 스타일 시안 캡처: `C:\Users\user\Desktop\hr-brief\work\aro-study-preview\`
