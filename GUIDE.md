# HR STUDY 모임 기록 사이트: 사용 안내

작성 2026-09-08, 갱신 2026-09-10. 총무(ARO)가 순서대로 보기 위한 문서다. 비밀번호는 여기에 적지 않는다.

## 0. 지금 상태 (2026-09-10)

- 끝난 것: Firebase 규칙 게시, 총무 등록, 멤버 등록(11명), 사이트 전체 동작 확인.
- 2026-09-09 첫 모임(OT)에서 정한 운영 방식이 사이트에 반영됐다: 매월 둘째 화요일 정기 모임, 발제 순서(11명 순환), 1인 회비 50,000원을 6개월로 나눠 월별 가용 금액 안에서 쓰고 덜 쓰면 이월, OT 비용은 참석자 n분의 1.
- 남은 것 (총무가 할 일, 순서대로):
  1. 실제 데이터 넣기: `../work/aro-study/setup-2026-09.json`에 위 내용이 준비돼 있다. 4-0절대로 `node scripts/setup.mjs`로 한 번에 넣는다. (admin 화면에서 하나씩 입력해도 결과는 같다.)
  2. 계좌를 개설하면 admin 회비 탭 > 회비 설정 "수정" > "입금 계좌"에 넣는다. 회비 현황에 표시된다.
  3. OT 지출의 "낸 사람"이 총무가 아니면 admin 지출 탭에서 그 항목을 "수정"해 바꾼다. 참석자가 11명 전원이 아니면 세션 탭에서 체크를 고친다.
  4. 첫 회비 입금이 들어오면 회비 탭에서 "납부 처리".
  5. OT 기록 페이지가 필요하면 모임 메모를 Claude Code에 주고 "9월 9일 기록 만들어"(5절).

## 1. 주소 모음

| 용도 | 주소 | 로그인 |
|---|---|---|
| 사이트(회비 현황·주차 목록) | https://aro-deeply.github.io/aro-study/ | 필요 |
| 총무 관리 화면 | https://aro-deeply.github.io/aro-study/admin.html | 필요(총무 이름) |
| 목록 화면 예시(가명 데이터) | https://aro-deeply.github.io/aro-study/?demo=1 | 없음 |
| 주차 페이지 예시(하이라이트·메모·의견 시험 가능) | https://aro-deeply.github.io/aro-study/demo/ | 없음 |
| GitHub 저장소 | https://github.com/aro-deeply/aro-study | GitHub 계정 |
| Firebase 콘솔 | https://console.firebase.google.com (프로젝트 hr-study-54acd) | Google 계정 |

예시 페이지 두 개는 실제 데이터와 무관하다. 화면 구성만 보는 용도이고, 거기서 남긴 메모·의견은 저장되지 않는다.

## 2. 사이트 구조 한눈에

- 로그인은 공용 비밀번호 하나. 그다음 "이름 선택"으로 본인을 고르면 의견·메모가 그 이름으로 기록된다. 기기마다 한 번만 하면 된다.
- 총무 권한은 별도 로그인이 아니라 "이름 선택"에서 총무로 등록된 이름을 골랐을 때 생긴다. 상단에 "관리" 링크가 보이면 총무 상태다.
- 화면은 세 종류다.
  - 목록 화면(첫 화면): 다음 모임 날짜·발제자·회비 잔액·이달 가용 금액 카드, 일정과 발제(12개월 표, 발제 순서), 회비 현황(월별 가용 금액 표, 멤버별 납부 표), 주차 목록. 추가 비용을 n분의 1로 나눈 게 있을 때만 그 정산 표가 맨 아래 붙는다.
  - 주차 페이지(`weeks/<날짜>/`): 이날의 내용, 브리프 링크, 다음 모임 계획, 이 회차 비용, 의견, 본문 하이라이트·메모. 모임 후 Claude Code로 만든다(5절).
  - 관리 화면(admin.html): 총무만. 멤버, 일정·발제, 회비, 세션, 지출, 송금 기록 6개 탭.
- 스터디 내용 기록은 관리 화면이 아니라 주차 페이지에서 한다. 내용 요약과 브리프 링크는 총무가 Claude Code로 만들고, 의견·하이라이트·메모는 멤버들이 그 페이지에서 직접 남긴다. 관리 화면 세션 탭의 "총무 메모"는 총무만 보는 짧은 메모 자리다.

## 3. 처음 한 번만 하는 설정 (끝났음, 다시 할 때 참고)

### 3-1. Firestore 규칙 게시

1. Firebase 콘솔에서 프로젝트 `hr-study-54acd`를 누른다.
2. 왼쪽 "프로젝트 바로가기" 아래 "Firestore"를 누른다. (예전 콘솔은 "빌드" 아래 "Firestore Database")
3. 위쪽 탭 "규칙"을 누르고 편집창 내용을 전부 지운 뒤 아래를 붙여 넣고 "게시". (저장소의 `firestore.rules`와 같은 내용)

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

의미: 공용 계정으로 로그인한 사람만 읽고 쓸 수 있다. 사이트의 실제 접근 제한은 이 규칙 하나다.

### 3-2. Authentication

Firebase 콘솔 > Authentication > Users 에 공용 계정이 있고, "Sign-in method"에 "이메일/비밀번호"가 켜져 있어야 한다. 2026-09-08 기준 확인됨.

### 3-3. 총무 등록

admin.html에서 "이름 없이 계속" > 멤버 탭에서 본인 이름을 "총무 (관리 권한)" 체크와 함께 저장 > 상단 "이름 선택"에서 본인을 고른다. "관리" 링크가 생기면 끝. 2026-09-09 완료.

## 4. 총무의 평소 사용 (admin.html)

### 4-0. 운영 데이터 한 번에 넣기 (scripts/setup.mjs)

2026-09-10에 정한 내용(모임 규칙, 발제 순서, 회비 설정, OT 세션과 지출 5건)은 `C:\Users\user\Desktop\hr-brief\work\aro-study\setup-2026-09.json`에 준비돼 있다. 저장소 밖 파일이라 GitHub에 올라가지 않는다.

1. 시작 메뉴에서 PowerShell을 열고 아래를 한 줄씩 입력한다.
   ```
   cd "C:\Users\user\Desktop\hr-brief\HR study"
   node scripts/setup.mjs ../work/aro-study/setup-2026-09.json --dry-run
   ```
2. "공용 계정 비밀번호:"가 뜨면 사이트 비밀번호를 입력하고 Enter (화면에 글자가 안 보이는 게 정상).
3. "넣을 내용" 목록이 나온다. 멤버 이름이 admin에 등록된 이름과 다르면 `멤버를 찾을 수 없음: "이름"`으로 멈추므로, admin 멤버 탭에서 이름을 맞추거나 JSON의 이름을 고친 뒤 다시 실행한다.
4. 목록이 맞으면 `--dry-run`을 빼고 다시 실행한다. 비밀번호를 넣고, "n건을 Firestore에 쓸까요? (y/N)"에 `y` Enter.
5. "끝."이 나오면 사이트를 새로고침한다. 첫 화면 상단에 "다음 모임 10월 13일 (화)"와 10월 발제자 이름(순서 2번), 회비 현황에 월별 가용 금액 표, 주차 목록에 OT 카드(지출 149,300원), 맨 아래 "추가 비용 정산" 표가 보이면 정상.
6. 두 번 실행해도 이미 있는 회비 설정·지출은 "이미 있음, 건너뜀"으로 표시되고 중복되지 않는다.

JSON에서 미리 정해 둔 가정(틀리면 admin에서 고치면 된다): 회비 기간은 2026년 10월부터 6개월(2027년 3월까지, 9월 OT는 회비 밖), OT 참석자는 활성 멤버 전원, OT 지출 5건의 낸 사람은 총무.

### 4-1. 운영 방식

- 정기 모임은 매월 둘째 화요일. 목록 화면의 "다음 모임"은 이 규칙으로 자동 계산되고, 그 달에 세션을 만들면 세션 날짜가 우선한다. 규칙은 일정·발제 탭에서 바꾼다.
- 발제는 정한 순서대로 매월 한 명. 한 바퀴 돌면 처음으로. 특정 달만 다른 사람이 하면 세션 탭에서 그 세션의 발제자를 고른다.
- 1인 회비를 걷어 총무가 보관하고, 모임 비용은 회비에서 쓴다. 잔액 = 납부 합계 - 회비 지출 합계.
- 월별 가용 금액: 회비 총액(1인 회비 x 대상 인원, 예: 50,000 x 11 = 550,000원)을 사용 개월 수(6)로 나눠 매월 91,667원을 배정하고, 덜 쓴 금액은 다음 달로 이월된다. 이달 가용 = 이달 배정 + 이월 - 이달 지출. 이 계산은 계획 총액 기준이라 미납자가 있으면 실제 현금(잔액 카드)은 그보다 적다.
- 총무가 아닌 사람이 모임 비용을 대신 결제하면 "회비에서 보전할 돈"으로 표시되고, 총무가 돌려준 뒤 송금 기록을 남기면 사라진다.
- 회비를 넘는 추가 비용과 OT처럼 회비 밖으로 정한 비용만 참석자끼리 n분의 1로 나눈다(100원 단위 내림, 뒷자리는 총무 부담). 이런 지출이 없으면 개인 정산 화면은 아예 나오지 않는다.

### 4-1-1. 일정·발제 탭

- 정기 모임 규칙: 몇째(첫째~넷째, 마지막), 요일, 시간, 장소. 저장하면 목록 화면 상단과 "일정과 발제" 표가 바뀐다.
- 발제 순서: "시작 월"을 고르고 멤버를 순서대로 넣는다("활성 멤버를 멤버 순서대로 채우기" 후 위로/아래로/빼기로 조정). 시작 월의 발제자가 1번. 아래 미리보기에서 12개월치 날짜와 발제자를 확인하고 저장.
- 멤버가 빠지거나 새로 들어오면 여기서 순서를 고친다. 이미 지난 달은 세션의 발제자로 남아 있으므로 순서를 바꿔도 과거 기록은 바뀌지 않는다(세션에 발제자가 저장된 경우).

### 4-2. 회비 탭

1. 처음 한 번: 아래쪽 "회비 설정 만들기"에서 1인 회비 금액, 시작일, 사용 개월 수(예: 6)를 넣고 저장한다. 종료일은 비우면 개월 수로 계산된다. 입금 계좌를 넣으면 회비 현황에 표시된다. 대상은 기본 "활성 멤버 전원"이라 나중에 추가한 멤버도 자동으로 들어간다.
2. 입금을 확인할 때마다 위쪽 납부 현황에서 그 사람 옆 "납부 처리"를 누른다. 오늘 날짜로 1인 회비 전액이 기록된다.
3. 금액이나 날짜가 다르면(나눠 냈거나 지난 날짜) "납부 기록 직접 입력"을 쓴다. 일부만 냈으면 "일부"로 표시되고 남은 금액이 보인다.
4. 잘못 눌렀으면 그 사람 옆 "취소". 회비 금액이나 기간이 바뀌면 회비 설정 목록에서 "수정". 다음 회비를 새로 걷을 때는 설정을 하나 더 만들면 된다(이전 설정은 목록 화면 아래 "이전 회비 설정"으로 남는다).

### 4-3. 세션 탭

모임 날짜(= 세션 ID = 주차 페이지 폴더명), 제목, 발제자(날짜를 고르면 순서상 발제자가 자동으로 들어가고 바꿀 수 있다), 참석자 체크, 다음 모임 계획, 총무 메모. 모임 당일 만들어 두면 지출 입력이 바로 된다. 세션을 삭제하면 딸린 지출·송금 기록도 함께 지워진다(확인창에 건수 표시). 의견·하이라이트는 남는다.

### 4-4. 지출 탭

위에서 세션을 고른 뒤 항목, 금액(숫자 키패드), 날짜, 카테고리(식사·카페·장소·기타), 재원, 낸 사람, 비고.

- 재원 기본값은 "회비에서". 개인 정산에 잡히지 않고 잔액만 줄어든다.
- 총무가 아닌 사람이 대신 결제했으면 "낸 사람"을 그 사람으로 둔다. 목록 화면에 "회비에서 보전할 돈"으로 뜬다.
- 회비 밖 추가 비용은 재원을 "참석자 n분의 1"로 바꾸고 분배 대상(기본 참석자 전원)을 고른다.
- 목록에서 "수정"을 누르면 고치거나 삭제할 수 있다.

### 4-5. 송금 기록 탭

- 대신 결제한 사람에게 회비에서 돌려줬을 때: 보낸 사람 "회비 (총무 보관)", 받은 사람 그 멤버.
- 추가 비용을 멤버끼리 보냈을 때: 보낸 사람과 받은 사람.
- 기록하면 "보전할 돈"과 "보낼 돈"에서 빠지고 완료 표시가 붙는다.

### 4-6. 멤버 탭과 총무 변경

- 멤버 추가는 이름만 넣으면 된다. 순서는 비워 두면 자동. 비활성화한 멤버는 이름 선택과 참석자 목록에서 빠지지만 과거 기록에는 남는다.
- 총무 변경: 새 총무의 "수정"을 눌러 "총무 (관리 권한)"을 체크해 저장하고, 본인 항목의 체크를 해제한다. 새 총무는 자기 기기에서 "이름 선택"으로 본인을 고르면 "관리" 링크가 생긴다. 비밀번호는 같은 공용 비밀번호다.
- 총무는 회비 보관자로 계산된다. 총무가 바뀌면 그 뒤로 "보전할 돈"과 "뒷자리 부담"이 새 총무 기준이 된다. 총무가 두 명이면 순서가 앞선 사람이 보관자로 계산되므로 평소에는 한 명만 둔다. 실제 계좌의 돈을 옮기는 것은 사이트 밖의 일이고 잔액 숫자는 그대로 이어진다.

## 5. 모임 후 기록 페이지 만들기 (Claude Code)

1. 이 폴더(`HR study`)에서 Claude Code를 연다.
2. 모임 메모(텍스트나 음성 전사)를 붙여 넣고 "이번 주 기록 만들어"라고 한다. 날짜와 제목을 물어보면 답한다.
3. Claude가 `weeks/<날짜>/index.html`을 만들고(내용 요약, 관련 브리프 링크 카드, 다음 모임 계획), Firestore 세션을 등록하고(이때 공용 비밀번호를 터미널에서 한 번 묻는다, 화면에 표시되지 않음), 커밋·푸시한 뒤 주소를 알려 준다.
4. 1~2분 뒤 목록 맨 위 카드가 링크로 바뀌면 끝. 참석자 체크나 지출 입력이 안 되어 있으면 admin에서 마저 한다.
5. 멤버들은 그 페이지에서 본문을 드래그해 하이라이트·메모를 남기고, 아래 "의견"에 댓글과 답글을 단다.

메모 원문은 `../work/aro-study/<날짜>/`에 남고(저장소 밖, 비공개), 저장소에는 정리본만 들어간다. 주차 페이지 본문에는 멤버 이름이나 민감한 내용을 쓰지 않는다(저장소가 공개라서).

## 6. 멤버에게 보낼 안내 (복사해서 쓰면 됨)

> 스터디 기록 사이트 주소: https://aro-deeply.github.io/aro-study/
> 처음 열면 비밀번호를 묻습니다. (비밀번호는 따로 알려 드립니다.) 한 번 넣으면 같은 기기에서는 다시 묻지 않습니다.
> 그다음 "이름 선택"에서 본인 이름을 고르세요. 의견과 메모가 그 이름으로 기록됩니다.
> 첫 화면에서 회비 납부 현황과 잔액을 볼 수 있습니다.
> 핸드폰에서는 브라우저 메뉴의 "홈 화면에 추가"를 누르면 앱처럼 쓸 수 있습니다.
> 각 주차 페이지에서 본문을 드래그하면 하이라이트·메모를 남길 수 있고, 아래 "의견"에 댓글을 달 수 있습니다.

## 7. 확인 체크리스트

끝난 것은 표시해 두었다. 나머지는 시간 날 때 위에서부터.

확인 완료 (2026-09-09)
- [x] 로그인 화면에 비밀번호 칸 하나만 보인다.
- [x] 로그인 후 검은 "권한 오류" 토스트가 뜨지 않는다.
- [x] admin에서 본인을 총무로 추가하고 이름을 고르면 상단에 "관리" 링크가 생긴다.
- [x] 세션을 만들고 지출을 넣으면 합계·차액 계산이 맞다. (n분의 1 방식으로 확인, 이후 회비 방식으로 변경)
- [x] 회비 탭에서 회비 설정을 만들면 멤버 전원이 "미납"으로 뜨고, "납부 처리"를 누르면 "납부"로, "취소"하면 다시 "미납"으로 바뀐다. 설정 삭제도 된다. (확인용 설정으로 시험 후 삭제)

아직 안 한 것
- [ ] 틀린 비밀번호를 넣으면 "비밀번호가 맞지 않습니다"가 뜬다.
- [ ] 총무가 아닌 이름으로 admin.html을 열면 "총무만 들어올 수 있는 화면입니다" 안내만 보인다. (시크릿 창에서 확인)
- [ ] 예시 페이지(demo)에서 본문을 드래그하면 "하이라이트 / 메모" 버튼이 뜨고, 의견을 남기고 답글·삭제가 된다.
- [ ] 핸드폰에서 첫 화면(회비 잔액 카드, 납부 표, 주차 목록)이 한 줄로 잘 접혀 보인다.
- [ ] 핸드폰 브라우저 "홈 화면에 추가"가 되고, 아이콘으로 열면 주소창 없이 뜬다.
- [ ] 실제 회비 설정을 만든 뒤: 목록 화면 상단 미납 인원과 회비 현황 표가 admin의 납부 처리와 같이 바뀐다.
- [ ] 첫 모임 후: 세션과 지출을 넣으면 목록 화면 회비 잔액이 그만큼 줄고, 주차 카드에 지출 합계가 뜬다.
- [ ] 지출의 "낸 사람"을 총무가 아닌 멤버로 두면 "회비에서 보전할 돈"에 뜨고, 송금 기록(보낸 사람 "회비")을 남기면 사라진다.
- [ ] 재원을 "참석자 n분의 1"로 넣은 지출이 있을 때만 목록 아래 "추가 비용 정산" 표가 나타난다.
- [ ] 두 기기 확인: 핸드폰에서 남긴 의견이 노트북 화면에 새로고침 없이 나타난다. (주차 페이지가 있어야 하므로 첫 기록 후에 확인)

## 8. 결정하거나 검토할 것 (총무 판단)

- 회비 조건(2026-09-10 확정): 1인 50,000원, 6개월 분할 사용, 덜 쓴 금액 이월. 미확정: 회비 기간의 시작 월(JSON은 10월로 가정), 입금 계좌(개설 후 입력), 6개월 뒤 남는 돈의 처리(이월·환급), 중도 합류·탈퇴 멤버의 회비.
- 발제 순서의 가정: 11명이 한 바퀴 돌면 다시 1번(9월 OT 발제자)부터. 순서가 다르면 일정·발제 탭에서 고친다.
- 비밀번호 변경 여부: 사이트 제작 과정에서 비밀번호가 Claude 대화에 한 번 입력됐다. 파일에는 남기지 않았다. 바꾸려면 Firebase 콘솔 > Authentication > Users > 계정 오른쪽 메뉴 > "비밀번호 재설정" 또는 새 계정 생성. 바꾸면 멤버 전원에게 새 비밀번호를 알려야 한다.
- 관리 권한 검증 수준: admin 진입은 화면(클라이언트)에서만 막는다. 공용 계정을 아는 사람이 개발자 도구를 쓰면 데이터를 고칠 수 있다. 스터디 규모에서는 감수하기로 한 설계(SPEC 4-4).
- 아이콘: 지금은 aro-briefs와 같은 아이콘을 쓴다. 홈 화면에서 두 앱을 구분하고 싶으면 `assets/icons/`의 PNG 3장을 바꾸면 된다.
- 카테고리: 식사·카페·장소·기타 4개. 바꾸려면 `admin.html`의 카테고리 목록과 `assets/settle.js`의 `CATEGORIES`를 같이 고친다.
- 공개 범위: 저장소는 공개(GitHub Pages 조건)다. 정적 HTML에는 모임 내용 요약만 들어가고 이름·금액·의견은 Firestore에만 있으므로, 주차 페이지 본문에 멤버 이름이나 민감한 내용을 쓰지 않는 원칙을 지키면 된다.

## 9. 문제 해결

- "권한 오류: Firestore 규칙을 확인하세요": 3-1 규칙이 풀린 것. 다시 게시 후 새로고침.
- "Firestore 인덱스가 필요합니다" 안내와 함께 콘솔 링크가 열림: 링크 페이지에서 "인덱스 만들기"를 누르고 몇 분 기다린 뒤 새로고침. (현재 설계에서는 필요 없게 만들었지만 혹시 뜨면 이렇게 한다.)
- 로그인은 되는데 이름 목록이 비어 있음: admin에서 멤버를 아직 안 넣은 것. "이름 없이 계속"으로 들어가 admin에서 추가.
- 첫 화면에 "아직 회비 설정이 없습니다": admin 회비 탭에서 1인 회비를 아직 안 넣은 것. 4-2 참고.
- 주차 페이지가 404: 푸시 후 1~2분 안 지났거나, 세션만 만들고 기록 페이지(5절)를 아직 안 만든 것. 목록에서 "기록 작성 전" 태그가 그 상태다.
- 화면이 예전 것처럼 보임: 브라우저 캐시. 새로고침을 두 번 하거나 시크릿 창으로 확인.
- 비밀번호를 잊음: Firebase 콘솔 > Authentication > Users 에서 재설정.
- 실수로 지출을 잘못 넣음: admin 지출 탭에서 해당 항목 "수정" > "삭제". 납부를 잘못 처리했으면 회비 탭에서 그 사람 옆 "취소".
- 세션을 지웠는데 잔액이나 정산에 숫자가 남아 있음: 2026-09-09 이후 버전에서는 세션 삭제 시 지출·송금 기록이 함께 지워지므로 생기지 않는다. 그래도 남아 있으면 같은 날짜로 세션을 다시 만들면 지출 탭에 나타나므로 거기서 지우고 세션을 다시 삭제한다.

## 10. 파일 위치 (참고)

- 이 폴더: `C:\Users\user\Desktop\hr-brief\HR study` (저장소 루트)
- 설계 원문 `SPEC.md`, 작업 규칙 `CLAUDE.md`, 사용 안내 `README.md`, 이 문서 `GUIDE.md`
- 화면 미리보기(로컬): 이 폴더에서 `python -m http.server 8770` 실행 후 브라우저에서 `http://127.0.0.1:8770/`. 파일을 더블클릭해서 여는 방식(file://)으로는 동작하지 않는다.
- 스타일 시안 캡처: `C:\Users\user\Desktop\hr-brief\work\aro-study-preview\`
