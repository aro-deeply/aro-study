# HR STUDY 모임 기록 사이트: 사용 안내와 확인 체크리스트

작성 2026-09-08. 이 문서는 총무(ARO)가 시간 날 때 순서대로 확인하기 위한 것이다. 비밀번호는 여기에 적지 않는다.

## 1. 주소 모음

| 용도 | 주소 | 로그인 |
|---|---|---|
| 사이트(목록·누적 정산) | https://aro-deeply.github.io/aro-study/ | 필요 |
| 총무 관리 화면 | https://aro-deeply.github.io/aro-study/admin.html | 필요(총무 이름) |
| 목록 화면 예시(가명 데이터) | https://aro-deeply.github.io/aro-study/?demo=1 | 없음 |
| 주차 페이지 예시(하이라이트·메모·의견 시험 가능) | https://aro-deeply.github.io/aro-study/demo/ | 없음 |
| GitHub 저장소 | https://github.com/aro-deeply/aro-study | GitHub 계정 |
| Firebase 콘솔 | https://console.firebase.google.com (프로젝트 hr-study-54acd) | Google 계정 |

예시 페이지 두 개는 실제 데이터와 무관하다. 화면 구성만 보는 용도이고, 거기서 남긴 메모·의견은 저장되지 않는다.

## 2. 처음 한 번만 하는 설정

현재 상태(2026-09-09): 2-1 규칙 게시와 2-3 총무 등록까지 끝났고 사이트 전체가 동작한다. 아래는 다시 해야 할 때를 위한 기록이다.

### 2-1. Firestore 규칙 게시 (필수, 5분)

1. Firebase 콘솔에 들어가 프로젝트 `hr-study-54acd`를 누른다.
2. 왼쪽 메뉴 "프로젝트 바로가기" 아래 "Firestore"를 누른다. (예전 콘솔에서는 "빌드" 아래 "Firestore Database")
   - "데이터베이스 만들기" 버튼이 보이면 아직 DB가 없는 것이다. 누르고, 위치는 `asia-northeast3 (Seoul)`, 모드는 "프로덕션 모드"를 고른 뒤 만든다.
3. 화면 위쪽 탭에서 "규칙"을 누른다.
4. 편집창 내용을 전부 지우고 아래를 그대로 붙여 넣는다. (저장소의 `firestore.rules` 파일과 같은 내용)

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

5. 편집창 위의 "게시" 버튼을 누른다. "규칙이 게시되었습니다" 안내가 뜨면 끝.
6. 확인: 사이트를 열어 비밀번호를 넣고 들어갔을 때 화면 아래에 "권한 오류" 검은 토스트가 뜨지 않으면 정상.

의미: 이 규칙은 "공용 계정으로 로그인한 사람만 읽고 쓸 수 있다"는 뜻이다. 사이트의 실제 접근 제한은 이 규칙 하나다.

### 2-2. Authentication 확인 (이미 되어 있음, 참고만)

Firebase 콘솔 > 빌드 > Authentication > Users 에 공용 계정 이메일이 있고, "Sign-in method" 탭에 "이메일/비밀번호"가 사용 설정되어 있어야 한다. 2026-09-08 기준 로그인이 성공했으므로 둘 다 되어 있다.

### 2-3. 총무 등록 (규칙 게시 직후)

1. https://aro-deeply.github.io/aro-study/admin.html 을 연다. 비밀번호를 넣는다.
2. "이름 선택" 화면에서 멤버가 없다는 안내가 뜨면 "이름 없이 계속"을 누른다.
3. 멤버 탭에서 본인 이름을 쓰고 "총무 (관리 권한)"이 체크된 상태로 "저장".
4. 화면 오른쪽 위 "이름 선택" 버튼을 눌러 본인을 고른다. 상단에 "관리" 링크가 생기면 총무로 인식된 것이다.
5. 나머지 멤버를 이름만 넣어 추가한다. 순서는 비워 두면 자동으로 붙는다.

## 3. 멤버에게 보낼 안내 (복사해서 쓰면 됨)

> 스터디 기록 사이트 주소: https://aro-deeply.github.io/aro-study/
> 처음 열면 비밀번호를 묻습니다. (비밀번호는 따로 알려 드립니다.) 한 번 넣으면 같은 기기에서는 다시 묻지 않습니다.
> 그다음 "이름 선택"에서 본인 이름을 고르세요. 의견과 메모가 그 이름으로 기록됩니다.
> 핸드폰에서는 브라우저 메뉴의 "홈 화면에 추가"를 누르면 앱처럼 쓸 수 있습니다.
> 각 주차 페이지에서 본문을 드래그하면 하이라이트·메모를 남길 수 있고, 아래 "의견"에 댓글을 달 수 있습니다.

## 4. 총무의 평소 사용 (admin.html)

운영 방식(2026-09-09 변경): 1인 회비를 걷어 총무 계좌에 두고, 모임 비용은 회비에서 쓴다. 목록 화면 맨 위에 회비 잔액, 미납자, 멤버별 납부 표가 뜬다. 회비를 넘는 추가 비용만 참석자끼리 n분의 1로 나눈다.

관리 화면은 5개 탭이다. 핸드폰에서 바로 쓸 수 있게 입력칸을 크게 만들었다.

- 멤버: 추가, 수정, 비활성화. 비활성 멤버는 이름 선택과 참석자 목록에서 빠지지만 과거 기록에는 남는다.
- 회비: 먼저 아래쪽 "회비 설정 만들기"에서 1인 회비를 넣고(이름·기간은 미정이면 비워도 됨, 대상은 기본 활성 멤버 전원) 저장한다. 그러면 위쪽에 멤버별 납부 현황이 뜨고, 입금 확인한 사람 옆의 "납부 처리"를 누르면 오늘 날짜로 전액 기록된다. 금액·날짜가 다르면 "납부 기록 직접 입력"을 쓴다. 잘못 눌렀으면 "취소". 회비 금액이나 기간이 바뀌면 회비 설정 목록에서 "수정".
- 세션: 모임 날짜(= 세션 ID = 주차 페이지 폴더명), 제목, 참석자 체크, 다음 모임 계획, 총무 메모. 모임 당일 만들어 두면 지출 입력이 바로 된다. 세션을 삭제하면 딸린 지출·송금 기록도 함께 지워진다.
- 지출: 위에서 세션을 고른 뒤 항목, 금액(숫자 키패드), 날짜, 카테고리(식사·카페·장소·기타), 재원, 낸 사람, 비고. 재원 기본값은 "회비에서"이고 이때는 개인 정산에 잡히지 않고 잔액만 줄어든다. 총무가 아닌 사람이 대신 결제했으면 "낸 사람"을 그 사람으로 두면 "회비에서 보전할 돈"에 뜬다. 회비 밖 추가 비용은 재원을 "참석자 n분의 1"로 바꾸고 분배 대상을 고른다. 목록에서 "수정"을 누르면 고치거나 삭제할 수 있다.
- 송금 기록: (1) 대신 결제한 사람에게 회비에서 돌려줬을 때, 보낸 사람 "회비 (총무 보관)", 받은 사람 그 멤버. (2) 추가 비용을 서로 보냈을 때, 보낸 사람과 받은 사람. 기록하면 "보전할 돈"과 "보낼 돈"에서 빠지고 완료 표시가 붙는다.

추가 비용 정산 규칙(기본값): 항목별 분배 대상에게 균등, 1인 부담은 100원 단위로 내림, 뒷자리는 총무 부담. 차액은 낸 금액에서 부담액을 뺀 값이고 +는 받을 돈, -는 낼 돈. 화면의 "중복 없음 · 합계 검산 완료"는 실제로 계산해서 표시하며, 안 맞으면 빨간 경고가 뜬다.

## 5. 모임 후 기록 페이지 만들기 (Claude Code)

1. 이 폴더(`HR study`)에서 Claude Code를 연다.
2. 모임 메모(텍스트나 음성 전사)를 붙여 넣고 "이번 주 기록 만들어"라고 한다. 날짜와 제목을 물어보면 답한다.
3. Claude가 `weeks/<날짜>/index.html`을 만들고, Firestore 세션을 등록하고(이때 공용 비밀번호를 터미널에서 한 번 묻는다, 화면에 표시되지 않음), 커밋·푸시한 뒤 주소를 알려 준다.
4. 1~2분 뒤 목록 맨 위 카드가 링크로 바뀌면 끝. 참석자 체크나 지출 입력이 안 되어 있으면 admin에서 마저 한다.

메모 원문은 `../work/aro-study/<날짜>/`에 남고(저장소 밖, 비공개), 저장소에는 정리본만 들어간다.

## 6. 확인 체크리스트

시간 날 때 위에서부터 하나씩. 예상 결과와 다르면 8절 문제 해결을 본다.

기본 동작 (규칙 게시 전에도 가능)
- [ ] 사이트를 열면 "HR STUDY 모임 기록" 로그인 화면에 비밀번호 칸 하나만 보인다. 이메일은 보이지 않는다.
- [ ] 틀린 비밀번호를 넣으면 "비밀번호가 맞지 않습니다"가 뜬다.
- [ ] 맞는 비밀번호로 들어간 뒤 새로고침해도 다시 묻지 않는다.
- [ ] 예시 페이지(demo)에서 본문을 드래그하면 "하이라이트 / 메모" 버튼이 뜨고, 메모를 남기면 본문 아래 메모 상자가 열린다. 하단 바에서 "전체"를 누르면 다른 사람(가명) 메모가 회색으로 보인다.
- [ ] 예시 페이지에서 의견을 남기고 답글을 달고 삭제할 수 있다. 다른 사람 글에는 삭제 버튼이 없다.
- [ ] 핸드폰에서 정산 화면(총 지출, 1인 카드, 카테고리 막대, 상세 내역, 보낼 돈)이 한 줄로 잘 접혀 보인다.
- [ ] 핸드폰 브라우저 "홈 화면에 추가"가 되고, 아이콘으로 열면 주소창 없이 뜬다.

규칙 게시 후
- [ ] 로그인 후 검은 "권한 오류" 토스트가 뜨지 않는다.
- [ ] admin에서 본인을 총무로 추가하고 이름을 고르면 상단에 "관리" 링크가 생긴다.
- [ ] 총무가 아닌 이름으로 admin.html을 열면 입력 화면 대신 "총무만 들어올 수 있는 화면입니다" 안내만 보인다.
- [ ] 회비 탭에서 회비 설정을 만들면 멤버 전원이 "미납"으로 뜨고, "납부 처리"를 누른 사람은 "납부"로 바뀐다. 목록 화면 상단 미납 인원과 회비 현황 표가 같이 바뀐다.
- [ ] 세션을 만들고 지출을 2~3건 넣으면 admin의 "이 세션의 지출" 합계가 맞고, 목록 화면 회비 잔액이 그만큼 줄어든다.
- [ ] 목록 화면에 그 세션 카드가 "기록 작성 전" 태그와 지출 합계와 함께 뜬다.
- [ ] 지출의 "낸 사람"을 총무가 아닌 멤버로 두면 "회비에서 보전할 돈"에 그 사람이 뜨고, 송금 기록(보낸 사람 "회비")을 남기면 사라진다.
- [ ] 재원을 "참석자 n분의 1"로 넣은 지출이 있을 때만 목록 아래 "추가 비용 정산" 표가 나타나고, 송금 기록을 남기면 "보낼 돈"이 줄어든다.
- [ ] 두 기기 확인: 핸드폰에서 남긴 의견이 노트북 화면에 새로고침 없이 나타난다. (주차 페이지가 있어야 하므로 첫 기록 후에 확인)

## 7. 결정하거나 검토할 것 (총무 판단)

- 비밀번호 변경 여부: 사이트 제작 과정에서 비밀번호가 Claude 대화에 한 번 입력됐다. 파일에는 남기지 않았다. 바꾸려면 Firebase 콘솔 > Authentication > Users > 계정 오른쪽 메뉴 > "비밀번호 재설정" 또는 새 계정 생성. 바꾸면 멤버 전원에게 새 비밀번호를 알려야 한다.
- 관리 권한 검증 수준: admin 진입은 화면(클라이언트)에서만 막는다. 공용 계정을 아는 사람이 개발자 도구를 쓰면 데이터를 고칠 수 있다. 스터디 규모에서는 감수하기로 한 설계(SPEC 4-4).
- 아이콘: 지금은 aro-briefs와 같은 아이콘을 쓴다. 홈 화면에서 두 앱을 구분하고 싶으면 `assets/icons/`의 PNG 3장을 바꾸면 된다.
- 카테고리: 식사·카페·장소·기타 4개. 바꾸려면 `admin.html`의 카테고리 목록과 `assets/settle.js`의 `CATEGORIES`를 같이 고친다.
- 회비 조건(2026-09-09 기준 미확정): 1인 금액, 기간(미정이면 비워 둠), 입금 계좌 주체, 남는 돈의 이월·환급. 정해지면 admin 회비 탭에서 회비 설정을 만들거나 수정하면 되고 코드 수정은 필요 없다. 이월·환급은 회비 설정 메모에 적어 둔다.
- 공개 범위: 저장소는 공개(GitHub Pages 조건)다. 정적 HTML에는 모임 내용 요약만 들어가고 이름·금액·의견은 Firestore에만 있으므로, 주차 페이지 본문에 멤버 이름이나 민감한 내용을 쓰지 않는 원칙을 지키면 된다.

## 8. 문제 해결

- "권한 오류: Firestore 규칙을 확인하세요": 2-1 규칙 게시가 안 된 것. 게시 후 새로고침.
- "Firestore 인덱스가 필요합니다" 안내와 함께 콘솔 링크가 열림: 링크 페이지에서 "인덱스 만들기"를 누르고 몇 분 기다린 뒤 새로고침. (현재 설계에서는 필요 없게 만들었지만 혹시 뜨면 이렇게 한다.)
- 로그인은 되는데 이름 목록이 비어 있음: admin에서 멤버를 아직 안 넣은 것. "이름 없이 계속"으로 들어가 admin에서 추가.
- 주차 페이지가 404: 푸시 후 1~2분 안 지났거나, 세션만 만들고 기록 페이지(5절)를 아직 안 만든 것. 목록에서 "기록 작성 전" 태그가 그 상태다.
- 화면이 예전 것처럼 보임: 브라우저 캐시. 새로고침을 두 번 하거나 시크릿 창으로 확인.
- 비밀번호를 잊음: Firebase 콘솔 > Authentication > Users 에서 재설정.
- 실수로 지출을 잘못 넣음: admin 지출 탭에서 해당 항목 "수정" > "삭제". 세션을 삭제하면 그 세션의 지출·정산 완료 기록도 함께 지워진다(확인창에 건수가 표시됨). 의견·하이라이트는 남는다.

## 9. 파일 위치 (참고)

- 이 폴더: `C:\Users\user\Desktop\hr-brief\HR study` (저장소 루트)
- 설계 원문 `SPEC.md`, 작업 규칙 `CLAUDE.md`, 사용 안내 `README.md`, 이 문서 `GUIDE.md`
- 화면 미리보기(로컬): 이 폴더에서 `python -m http.server 8770` 실행 후 브라우저에서 `http://127.0.0.1:8770/`. 파일을 더블클릭해서 여는 방식(file://)으로는 동작하지 않는다.
- 스타일 시안 캡처: `C:\Users\user\Desktop\hr-brief\work\aro-study-preview\`
